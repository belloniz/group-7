apiVersion: v1
kind: Pod
metadata:
  name: fastfoodapi-pod
  labels:
    app: fastfoodapi-pod
spec:
  containers:
  - name: fastfoodapi
    image: brwillon/fastfoodapi
    ports:
      - name: api-port
        containerPort: 80
